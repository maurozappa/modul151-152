export function getForm(addresses) {
    let address = {
        id: '',
        firstname: '',
        lastname: '',
        street: '',
        city: '',
        country: '',
      };
      if (id) {
          address = addresses.firstname((adr) => adr.id )
      }
}